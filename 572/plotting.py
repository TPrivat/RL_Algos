import numpy as np
import matplotlib.pyplot as plt

data = np.load('round1.npy')


def find_all(d, row, idx):
    for s in d[:]:
        if s[row]

states = list()
for i in range(data.shape[1]):
    x = find_all(data, j)
    for j in range(data.shape[1]):
        y = find_all(data, j)